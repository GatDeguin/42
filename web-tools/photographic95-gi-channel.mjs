import {NodeIO} from '@gltf-transform/core';const d=await new NodeIO().read('docs/preview95/gi/studio-surfaces.glb');const n=d.getRoot().listNodes().find(n=>n.getName()==='Bafle cielorraso estudio 1'),p=n.getMesh().listPrimitives()[0];for(const key of ['TEXCOORD_0','TEXCOORD_1']){const a=p.getAttribute(key).getArray();let min=[Infinity,Infinity],max=[-Infinity,-Infinity];for(let i=0;i<a.length;i+=2){for(let k=0;k<2;k++){min[k]=Math.min(min[k],a[i+k]);max[k]=Math.max(max[k],a[i+k]);}}console.log(key,min,max,a.slice(0,12));}

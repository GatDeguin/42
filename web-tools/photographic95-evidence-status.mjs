import fs from 'node:fs';const f='docs/preview95/benchmarks.json';const r=JSON.parse(fs.readFileSync(f));r.visualVerification={status:'REJECTED — captured before dynamic-attribute decoding fix',reason:'Quantized dynamic geometry entered the BVH as raw integer values up to +/-32767. The shader ran but the light solution was wrong.',fix:'Decode every dynamic vertex attribute to Float32 before scene generation; assert generated bounds are within visible scene bounds.',pending:'Repeat visual and functional tests after GPU is released by the parent Cycles run.'};fs.writeFileSync(f,JSON.stringify(r,null,2));

import {NodeIO} from '@gltf-transform/core';import {ALL_EXTENSIONS} from '@gltf-transform/extensions';import {MeshoptDecoder} from 'meshoptimizer';await MeshoptDecoder.ready;const d=await new NodeIO().registerExtensions(ALL_EXTENSIONS).registerDependencies({'meshopt.decoder':MeshoptDecoder}).read('docs/preview95/assets/house.glb');console.log(d.getRoot().listScenes().map(s=>({name:s.getName(),nodes:s.listChildren().length,default:s===d.getRoot().getDefaultScene()})));
